# First launch requires App language and required ASR Models

The Library is blocked until the user picks App language (default Persian) and every file of each required ASR Model is on disk. LLM Provider setup is offered on the same screen and is optional. Skipping the download would let Captioning fail in the main window with no model; making Provider required would block a Search-only user.

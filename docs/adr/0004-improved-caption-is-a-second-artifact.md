# Improved Caption is stored beside Caption, not instead of it

ASR and imported files misspell technical terms, so an LLM may rewrite wording for Search, Ask, Summary, and subtitles. That rewrite is a second timed artifact (Improved Caption). Replacing Caption in place would destroy the only copy that can be re-improved without re-running ASR.

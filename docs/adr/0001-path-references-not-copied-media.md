# Videos are path references; the Library stores derived text

A Video points at a file the user already has. The Library stores Caption, Summary, and other derived text — never a copy of the media. Copying would duplicate large downloads they already keep; a path can break, so the Video record outlives a missing file and can be relinked or deleted.

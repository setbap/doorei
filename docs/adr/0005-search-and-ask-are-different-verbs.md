# Search and Ask are different verbs

Search is local retrieval: a list of Hits, no LLM. Ask is a chat that needs an LLM and cites Hits. Merging them into one box would make retrieval unusable when no LLM is configured, and would hide jump-to-timestamp behind generated prose.
